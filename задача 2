#include<iostream>
using namespace std;
int fib(int N) {
	if (N == 1 || N == 2)
		return 1;
	else
		return fib(N - 1) + fib(N - 2);
}
int main() {
	setlocale(LC_ALL, "Russian");
	int N; 
	cout << "Введите порядковый номер элемента: " << endl;
	cin >> N;
	cout << fib(N) << endl;
	return 0;
}
